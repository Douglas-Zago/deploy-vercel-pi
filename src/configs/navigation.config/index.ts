import paceNavigationConfig from './pace.navigation.config'

import type { NavigationTree } from '@/@types/navigation'

const navigationConfig: NavigationTree[] = [...paceNavigationConfig]

export default navigationConfig
